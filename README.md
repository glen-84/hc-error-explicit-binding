# hc-error-explicit-binding

- Run `dotnet watch --no-hot-reload`.
- Switch to explicit binding on line 9 in `Program.cs`.
- See error:
    ```
    1. The field `code` must be implement by object type `TestError`.
    ```